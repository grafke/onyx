"""
Lithuanian translations of Onyx prompt modules.

This package mirrors `onyx.prompts` but with prompt texts translated to Lithuanian.
Programmatic sentinels (e.g., JSON keys, exact match tokens) are preserved when
referenced by parsing code elsewhere in the codebase.
"""
